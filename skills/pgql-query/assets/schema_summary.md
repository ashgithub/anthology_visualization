# Property graph schema summary

Use the active property graph and the DDL-derived type context supplied by the application.

## Guidance
- The app provides the active property graph name.
- The app provides vertex type summaries, edge type summaries, and selected-type relationship summaries when available.
- Use those graph artifacts to write PGQL queries.
